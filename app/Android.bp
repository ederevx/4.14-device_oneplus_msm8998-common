android_app_import {
    name: "Camera",
    product_specific: true,

    apk: "Camera.apk",
    preprocessed: true,

    overrides: [
        "Aperture",
    ],

    optional_uses_libs: ["androidx.camera.extensions.impl"],
}
